html
  head
    title Lista de productos
  body
    h1 Lista de productos

    form(action='/logout' method='post')
      button(type='submit') Cerrar sesión

    each product in products
      p= product.name + ' - Stock: ' + product.stock + ' - Precio: ' + product.price
    form(action='/products/add' method='post')
      input(type='text' name='name' placeholder='Nombre de producto')
      input(type='number' name='stock' placeholder='Stock')
      <input type="number" name="price" placeholder="Precio" step="0.01" min="0">
      button(type='submit') Añadir producto